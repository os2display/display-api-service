---
resources:
  App\Entity\Feed:
    attributes:
      input: App\Dto\FeedInput
      output: App\Dto\Feed
    itemOperations:
      get:
        openapi_context:
          operationId: get-feeds-id
          description: Retrieves a Feed resource.
          summary: Retrieve a Feed resource.
          tags:
            - Feeds
          parameters:
            - schema:
                type: string
                format: ulid
                pattern: "^[A-Za-z0-9]{26}$"
              name: id
              in: path
              required: true
          responses:
            '200':
              description: OK
              content:
                application/ld+json:
                  examples:
              headers: { }

      put:
        openapi_context:
          description: Update a Feed resource.
          summary: Update a Feed resource.
          operationId: put-v1-feed-id
          tags:
            - Feeds
          parameters:
            - schema:
                type: string
                format: ulid
                pattern: "^[A-Za-z0-9]{26}$"
              name: id
              in: path
              required: true

      delete:
        openapi_context:
          description: Delete a Feed resource.
          summary: Delete a Feed resource.
          operationId: delete-v1-feed-id
          tags:
            - Feeds
          parameters:
            - schema:
                type: string
                format: ulid
                pattern: "^[A-Za-z0-9]{26}$"
              name: id
              in: path
              required: true

    collectionOperations:
      get:
        filters: ['entity.search_filter', 'entity.order_filter', 'created.at.order_filter', 'updated.at.order_filter']
        openapi_context:
          operationId: get-v1-feeds
          description: Retrieves a collection of Feed resources.
          summary: Retrieves a collection of Feed resources.
          tags:
            - Feeds
          parameters:
            - schema:
                type: integer
                minimum: 0
                format: int32
                default: 1
              in: query
              name: page
            - schema:
                type: string
                default: '10'
              in: query
              name: itemsPerPage
              description: The number of items per page
          responses:
            '200':
              description: OK
              content:
                application/ld+json:
                  examples:

              headers: { }

      post:
        openapi_context:
          operationId: create-v1-feeds
          description: Creates a Feed resource.
          summary: Creates a Feed resource.
          tags:
            - Feeds
