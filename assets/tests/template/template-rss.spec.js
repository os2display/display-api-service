import { test, expect } from "@playwright/test";

test("RSS 0", async ({ page }) => {
  await page.goto(
    "/template/rss-0"
  );

  // TODO
});

test("RSS 1", async ({ page }) => {
  await page.goto(
    "/template/rss-1"
  );

  // TODO
});
