import { test, expect } from "@playwright/test";

test("Travel 0", async ({ page }) => {
  await page.goto(
    "/template/travel-0"
  );

  // TODO
});

test("Travel 1", async ({ page }) => {
  await page.goto(
    "/template/travel-1"
  );

  // TODO
});
